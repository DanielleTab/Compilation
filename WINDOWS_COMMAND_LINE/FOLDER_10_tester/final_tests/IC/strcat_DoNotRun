class PRINT
{
	void printInt(int i)
	{
	}
}

class MainClass
{
	void main(string[] args)
	{
		string str = "1";
		string str2 = "2";
		string strConcat = null;
		
		// add call to strcat here by adding the following lines 
		// in line 157 of the pseudo MIPS file:
		/*
			lw Temp_200, 0(Temp_30)
			addi $sp, $sp, -4
			sw Temp_200, 0($sp)			
			lw Temp_200, 0(Temp_26)
			addi $sp, $sp, -4
			sw Temp_200, 0($sp)
			jal Label_0_strcat
			addi $sp, $sp, 8
			mov Temp_34, $v0
		*/
		
		PRINT printer;
		printer.printInt(1);
	}
}